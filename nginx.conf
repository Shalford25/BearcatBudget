events{}
http {
    server {
        listen 4470;
        location / {
            try_files $uri =404;
            root home/shalford/BearcatBudget/data/www;
            index index.html;

        }
        location ~ \.(gif|jpg|png)$ {
            root /data/assets;
        }
    }
}
